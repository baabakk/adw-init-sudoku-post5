import { Board, Difficulty, PuzzleResponse, ValidateResponse } from '@init-sudoku-post5/contracts';

export type SudokuBoard = Board;
export type PuzzleDifficulty = Difficulty;
export type GetPuzzleResponse = PuzzleResponse;
export type ValidateBoardResponse = ValidateResponse;
